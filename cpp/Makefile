CXX             = g++
CXXFLAGS        = -c
OPTFLAG         = 
DEBUGFLAG       =
LINKER          = g++


SRC = \
    passwd.cpp \
    ls.cpp 

all: ls passwd

passwd:
	g++ passwd.cpp -o passwd

ls: 
	g++ ls.cpp -o ls

clean:
	rm ls passwd
